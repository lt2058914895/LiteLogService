server:
  port: 8080

spring:
  profiles:
    active: ${ENV:local}

jwt:
  secret: REDACTED
  expiration: 86400000

logging:
  level:
    com.example.litelog: INFO
    org.springframework.security: INFO
